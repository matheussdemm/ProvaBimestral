@model IEnumerable<ProvaBimestral.Models.Alternativas>

@{
    ViewData["Title"] = "Index";
}

<h1>Lista de Alternativas</h1>

<p>
    <a asp-action="Create">Criar novas alternativas</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Id_Pergunta)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Correta)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AlternativaTexto)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Id_Pergunta)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Correta)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AlternativaTexto)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>
